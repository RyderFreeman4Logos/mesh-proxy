heading-usage = Usage
heading-commands = Commands
heading-arguments = Arguments
heading-options = Options
label-default = default
label-possible-values = possible values
label-command = COMMAND
label-options = OPTIONS
label-help = Print help
label-version = Print version

command-root-about = Decentralized P2P port forwarding and service discovery
command-help-about = Print this message or the help of the given subcommand(s)
command-start-about = Start the mesh-proxy daemon.
command-restart-about = Restart the daemon using the current config.
command-stop-about = Stop the running daemon.
command-status-about = Show daemon status.
command-expose-about = Expose a local service to the mesh network.
command-unexpose-about = Remove a previously exposed local service from the mesh config.
command-accept-about = Accept an edge node's join ticket (control node only).
command-join-about = Join a mesh network using an invite token from the control node.
command-invite-about = Generate an invite token for a new edge node (control node only).
command-quota-about = Manage per-node service quotas (control node only).
command-quota-show-about = Show quota usage for all accepted nodes.
command-quota-set-about = Update the quota limit for a specific node.
command-install-service-about = Install a system service to start mesh-proxy on boot.

arg-config-help = Path to config file.
arg-config-value = CONFIG
arg-start-role-help = Node role: control or edge.
arg-start-role-value = ROLE
arg-start-control_addr-help = Serialized control node endpoint address written to config before startup.
arg-start-control_addr-value = CONTROL_ADDR
arg-status-output-help = Output format.
arg-status-output-value = OUTPUT
arg-expose-name-help = Service name (unique identifier on this node).
arg-expose-name-value = NAME
arg-expose-addr-help = Local address the service listens on (for example "127.0.0.1:8080" or "/tmp/foo.sock").
arg-expose-addr-value = ADDR
arg-expose-protocol-help = Transport protocol.
arg-expose-protocol-value = PROTOCOL
arg-expose-health_check_mode-help = Health check mode (tcp_connect, unix_connect, http_get).
arg-expose-health_check_mode-value = HEALTH_CHECK_MODE
arg-expose-health_check_target-help = Health check target (overrides addr for probe, for example an HTTP URL).
arg-expose-health_check_target-value = HEALTH_CHECK_TARGET
arg-unexpose-name-help = Service name to remove.
arg-unexpose-name-value = NAME
arg-accept-ticket-help = The join ticket generated by the edge node.
arg-accept-ticket-value = TICKET
arg-accept-name-help = Optional friendly name for the edge node.
arg-accept-name-value = NAME
arg-join-token-help = The invite token from `mesh-proxy invite`.
arg-join-token-value = TOKEN
arg-invite-name-help = Friendly name for the joining node.
arg-invite-name-value = NAME
arg-invite-ttl-help = Token validity duration in seconds (default: 300, max: 3600).
arg-invite-ttl-value = TTL
arg-quota-set-endpoint_id-help = Endpoint ID of the accepted node.
arg-quota-set-endpoint_id-value = ENDPOINT_ID
arg-quota-set-limit-help = New service quota limit.
arg-quota-set-limit-value = LIMIT
arg-install-service-user-help = Install as user service instead of system-wide (Linux: systemd --user, macOS: ~/Library/LaunchAgents).
